# ============================================================
# Text-to-SQL AI Agent — Requirements
# Compatible with Streamlit Community Cloud (Python 3.14)
#
# Key changes vs v1:
#   • pyarrow        → unpinned  (no 3.14 wheel exists for 16.1.0)
#   • pandas         → >= instead of == (avoids source-build on 3.14)
#   • numpy          → >= instead of == (same reason)
#   • langchain*     → >= instead of == (latest stable pulled by pip)
#   • streamlit      → >= (Cloud manages its own compatible version)
#   • streamlit-chat → removed (unmaintained, caused install errors)
#   • jupyter/ipykernel → removed (not needed at runtime on Cloud)
# ============================================================

# --- Core Data ---
pandas>=2.2.0
numpy>=1.26.0

# --- Database ---
sqlalchemy>=2.0.30
pymysql>=1.1.1
cryptography>=42.0.0          # Required by PyMySQL for TiDB SSL

# --- Data Fetching (notebook only, harmless to include) ---
kagglehub>=0.2.9

# --- LangChain Ecosystem ---
langchain>=0.2.16
langchain-community>=0.2.16
langchain-openai>=0.1.25      # OpenAI-compatible wrapper used for Grok
langchain-experimental>=0.0.64

# --- Streamlit UI ---
streamlit>=1.36.0

# --- Utilities ---
python-dotenv>=1.0.1
tabulate>=0.9.0
plotly>=5.22.0

# --- Arrow: unpinned so pip selects a wheel matching Python 3.14 ---
pyarrow>=15.0.0

# ── Dev/notebook only — NOT needed on Streamlit Cloud ──────────────────────
# jupyter
# ipykernel
